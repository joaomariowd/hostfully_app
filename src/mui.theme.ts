import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#bad0e3',
      contrastText: '#3f3f3f',
    },
    secondary: {
      main: '#feee7d',
    },
  },
});

export default theme;